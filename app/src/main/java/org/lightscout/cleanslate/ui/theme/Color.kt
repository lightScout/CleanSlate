package org.lightscout.cleanslate.ui.theme

import androidx.compose.ui.graphics.Color

// Primary colors - More vibrant purples with better contrast
val Purple80 = Color(0xFFBB86FC) // Brighter purple for dark theme
val PurpleGrey80 = Color(0xFFD0BCFF) // Lighter purple accent for dark theme
val Pink80 = Color(0xFFFF93C7) // Brighter pink for dark theme

val Purple40 = Color(0xFF6200EE) // Vibrant primary for light theme
val PurpleGrey40 = Color(0xFF7F67BE) // Richer secondary for light theme
val Pink40 = Color(0xFFE91E63) // Stronger pink accent for light theme

// Surfaces with elevation tonal values - with slightly more color
// Light theme surfaces with subtle primary tint
val SurfaceLight = Color(0xFFFAF9FD) // Very subtle purple tint
val SurfaceContainerLight = Color(0xFFF3EEFF) // Slightly elevated with purple undertone
val SurfaceContainerHighLight = Color(0xFFECE4FF) // Higher elevation with stronger tint

// Dark theme surfaces with better definition
val SurfaceDark = Color(0xFF121212) // Richer dark surface
val SurfaceContainerDark = Color(0xFF1E1E2E) // Dark with subtle purple undertone
val SurfaceContainerHighDark = Color(0xFF2D2C3F) // Higher elevation with more character
